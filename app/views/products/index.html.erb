<p>All flowers:</p>
<br>
<div class="columns is-multiline">
    <% @products.each do |f| %>
    <div class="column is-one-quarter">
        <div>
            <%= link_to "#{f.flower_colour.colour_name} #{f.flower_type.type_name}".titleize, f %>
            <% if f.image.present? %>
        </div>
    </div>
    <% end %>
</div>